---
summary: "CLI reference for `oni clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `oni clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `oni clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `oni clawbot qr` (same behavior as [`oni qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `oni clawbot qr` -> `oni qr`
