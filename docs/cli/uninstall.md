---
summary: "CLI reference for `oni uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `oni uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
oni uninstall
oni uninstall --all --yes
oni uninstall --dry-run
```
